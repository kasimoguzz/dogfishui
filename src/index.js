import React from 'react'
import styles from './styles.module.css'

export const ParagrafComponent = ({ text }) => {
  return <p>{text}</p>
}


export const BtnComponent = ({ text }) => {
  return <button>{text}</button>
}
